# Legacy Node/Postgres Archive

Ce dossier contient l'ancien stack Node/Express + PostgreSQL.

Contenu archive:
- `backend/`
- `db/`
- `docker-compose.yml`
- `alter_table.sql`
- `package.json`
- `package-lock.json`

Le chemin principal du projet est maintenant:
- backend: `spacetimedb/spacetimedb` (Rust + SpacetimeDB)
- frontend: `frontend/`

Ce dossier est conserve pour reference/historique uniquement.
