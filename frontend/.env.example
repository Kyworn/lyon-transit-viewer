# Source maps (set to false in production)
GENERATE_SOURCEMAP=false

# SpacetimeDB connection
# Local dev: http://127.0.0.1:3000
# Public deploy (single hostname behind Cloudflare Tunnel): https://tcl.zorko.xyz
REACT_APP_SPACETIMEDB_URI=http://127.0.0.1:3000
REACT_APP_SPACETIMEDB_DB=lyon-transit

# Map tiles: MapLibre GL + CARTO dark-matter (free, no API key required).
# No Mapbox token needed anymore.
